## 1. Lazy Gallery Loading

- [x] 1.1 Implement lazy cache loading of `market1501_gallery_features.npy` inside [search_gallery](file:///c:/Users/VICTUS/Documents/RecognitionCourse/in-allies-eyes/app.py#L112) in [app.py](file:///c:/Users/VICTUS/Documents/RecognitionCourse/in-allies-eyes/app.py).
- [x] 1.2 Cache the loaded dictionary globally in memory to avoid reading from disk on subsequent searches.

## 2. Cosine Similarity Search and Display

- [x] 2.1 Calculate the Cosine similarity metric between the normalized query feature and all gallery embeddings in the database.
- [x] 2.2 Retrieve and load the top matching gallery images from disk and render them in the baseline gallery dashboard output.
